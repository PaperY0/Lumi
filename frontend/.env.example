# Backend API base.
# Local development usually talks to the backend directly:
VITE_AI_API_BASE=http://localhost:3001

# For Docker/nginx same-origin proxy builds, set this to an empty string:
# VITE_AI_API_BASE=
